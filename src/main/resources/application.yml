info:
  app:
    name: User Management
    description: Manage user with SpringBoots
    version: 1.0
  author:
    name: PhiNX
    age: 20

spring:
  profiles:
    active: dev
  servlet:
    multipart:
      max-file-size: 50MB
  main:
    banner-mode: off

springdoc:
    default-produces-media-type: application/json

server:
  servlet:
    context-path: /api/v1/

# Debug
#logging:
#  level:
#    org:
#      springframework: DEBUG

logging:
  level:
    root: warn
    org:
      springframework:
        jdbc:
          core: TRACE
      hibernate:
        SQL: trace
        orm:
          jdbc:
            bind: trace